package workout.workoutSession;

import java.util.ArrayList;
import java.util.Scanner;

public class WorkoutSessionParser {
    public static void workoutSessionProcessCommand(){
        Scanner in = new Scanner(System.in);
        ArrayList<String> input = new ArrayList<String>;
        workoutSessionParser(in.nextLine().trim(), input);

        switch (input.toLowerCase(){
        case "add":

        }
    }

    private static void workoutSessionParser(String input, ArrayList<String> output){
        output
    }
}
